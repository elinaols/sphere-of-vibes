export interface ArtistProps {
    name: string,
    imageUrl: string,
    followers?: number,
    genre?: string,
    date?: string,
    album?: string,
    artistName?: string,
    playlistOwner?: string,
    externalUrl?: string,
}